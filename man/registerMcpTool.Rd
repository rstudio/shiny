% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp-tools.R
\name{registerMcpTool}
\alias{registerMcpTool}
\title{Register an MCP tool the model can call directly}
\usage{
registerMcpTool(...)
}
\arguments{
\item{...}{One or more tools created with \code{\link[ellmer:tool]{ellmer::tool()}}.}
}
\value{
Invisibly, \code{NULL}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

When a Shiny app is served as an MCP App (via \code{mcpConfigure()}),
\code{registerMcpTool()} exposes plain R functions as MCP tools the model may
call directly, in addition to the tool that opens the app. Define each
tool with \code{\link[ellmer:tool]{ellmer::tool()}} and register it at the top level of your
\code{app.R}, before the app starts:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{library(shiny)
mcpConfigure()

registerMcpTool(
  ellmer::tool(
    function(n) \{
      x <- rnorm(n)
      list(n = n, mean = mean(x), sd = stats::sd(x))
    \},
    name = "get_sample_stats",
    description = "Summary statistics for a normal sample of size n.",
    arguments = list(n = ellmer::type_integer("Sample size", required = TRUE))
  )
)
}\if{html}{\out{</div>}}

Handlers run in the server R process, outside of any Shiny session, and
may return a character vector (sent as text), a list (sent as JSON
\code{structuredContent}), or a promise. Errors are reported to the model as
tool errors.

Tools are keyed by their \code{name}; registering a tool whose name is already
registered replaces it. A tool may not use a name reserved by Shiny's MCP
server (the app-opening tool or the internal \verb{_shiny_*} tunnel tools).
}
\seealso{
\code{\link[=mcpUpdates]{mcpUpdates()}} and the other \verb{mcp*} session helpers.
}
